[workspace]
# Path to the workspace changelog. release-plz updates this single file.
changelog_path = "CHANGELOG.md"
# Keep the root CHANGELOG.md up to date.
changelog_update = true
# Enable automatic GitHub releases.
git_release_enable = true
# Create a production release (not a draft).
git_release_type = "prod"
# Publish to crates.io.
publish = true
# Do not publish if the working directory has uncommitted changes.
publish_allow_dirty = false
# Timeout for each `cargo publish` invocation.
publish_timeout = "10m"
# Release PR is not a draft.
pr_draft = false
# Labels to attach to the release PR.
pr_labels = ["release"]

# crates.io rate-limits new crates to 5 per day. To publish all workspace
# crates, release them in batches by temporarily setting publish=false below.
# Batch 1 (today): ocas-core, ocas-domain, ocas-poly, ocas-atom, ocas-parse
# Batch 2 (24h):    ocas-rewrite, ocas-calc, ocas-eval, ocas-gpl, ocas
# Batch 3 (48h):    ocas-c, ocas-py

# === Batch 2/3: temporarily skipped to respect crates.io new-crate rate limit ===
[[package]]
name = "ocas-rewrite"
publish = false

[[package]]
name = "ocas-calc"
publish = false

[[package]]
name = "ocas-eval"
publish = false

[[package]]
name = "ocas"
publish = false

[[package]]
name = "ocas-c"
publish = false

# ocas-gpl is an optional GPL backend and is not published to crates.io.
# Note: if the top-level `ocas` crate is ever published to crates.io, this
# crate must also be published because `ocas` optionally depends on it.
[[package]]
name = "ocas-gpl"
publish = false

# ocas-py is a Python extension module (cdylib) and is published to PyPI, not crates.io.
[[package]]
name = "ocas-py"
publish = false

# ocas-tests is an internal integration/benchmark crate.
[[package]]
name = "ocas-tests"
publish = false
